# ============================================================
#  Nutrition Agent — Python Dependencies
#  IBM Watsonx.ai + Flask Backend
#  Tested with Python 3.13 on Windows
# ============================================================

# Web Framework
Flask==3.0.3
Flask-CORS==4.0.1
Werkzeug==3.0.3

# IBM Watsonx.ai SDK
# 1.3.42 is the latest stable release whose pandas constraint (<2.3.0)
# ships a pre-built wheel for Python 3.13 — no C compiler required.
ibm-watsonx-ai==1.3.42

# Environment variable management
python-dotenv==1.0.1

# HTTP requests
requests==2.32.3

# Production WSGI server (Linux/macOS only; skip on Windows dev)
# gunicorn==22.0.0
